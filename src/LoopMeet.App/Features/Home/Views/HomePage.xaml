<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="LoopMeet.App.Features.Home.Views.HomePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Home">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="12">
            <HorizontalStackLayout Spacing="16" VerticalOptions="Center">
                <Border
                    WidthRequest="56"
                    HeightRequest="56"
                    StrokeThickness="0"
                    BackgroundColor="{StaticResource Gray400}">
                    <Border.StrokeShape>
                        <Ellipse />
                    </Border.StrokeShape>
                    <Grid WidthRequest="56" HeightRequest="56">
                        <Label
                            Text="{Binding UserInitial}"
                            FontSize="28" FontAttributes="Bold"
                            TextColor="White"
                            HorizontalOptions="Center" VerticalOptions="Center" />
                        <Image
                            Source="{Binding AvatarUrl}"
                            Aspect="AspectFill"
                            IsVisible="{Binding HasAvatar}" />
                    </Grid>
                </Border>
                <VerticalStackLayout Spacing="4" VerticalOptions="Center">
                    <Label Text="{Binding Title}" FontSize="24" FontAttributes="Bold" />
                    <Label Text="{Binding Headline}" FontSize="18" />
                </VerticalStackLayout>
            </HorizontalStackLayout>
            <Border
                Padding="16"
                Style="{StaticResource BubbleCardBorderStyle}">
                <VerticalStackLayout Spacing="8">
                    <Label Text="🏠" FontSize="28" HorizontalOptions="Center" />
                    <Label Text="{Binding SupportingText}" HorizontalTextAlignment="Center" />
                </VerticalStackLayout>
            </Border>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
