<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="LoopMeet.App.Features.Profile.Views.ProfilePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Profile">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="12">
            <Label Text="Profile" FontSize="20" FontAttributes="Bold" />

            <HorizontalStackLayout Spacing="16" VerticalOptions="Start">
                <Border
                    WidthRequest="56"
                    HeightRequest="56"
                    StrokeThickness="0"
                    BackgroundColor="{StaticResource Gray400}">
                    <Border.StrokeShape>
                        <Ellipse />
                    </Border.StrokeShape>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding PickAvatarCommand}" />
                    </Border.GestureRecognizers>
                    <Grid WidthRequest="56" HeightRequest="56">
                        <Label
                            Text="{Binding UserInitial}"
                            FontSize="28" FontAttributes="Bold"
                            TextColor="White"
                            HorizontalOptions="Center" VerticalOptions="Center" />
                        <Image
                            Source="{Binding AvatarUrl}"
                            Aspect="AspectFill"
                            IsVisible="{Binding HasAvatar}" />
                    </Grid>
                </Border>
                <VerticalStackLayout Spacing="4" VerticalOptions="Center">
                    <Label Text="Display name" FontAttributes="Bold" />
                    <Entry Text="{Binding DisplayName}" Placeholder="Display name" />
                </VerticalStackLayout>
            </HorizontalStackLayout>

            <Label Text="Email" FontAttributes="Bold" />
            <Label Text="{Binding Email}" />

            <Label Text="{Binding UserSince, StringFormat='User since: {0:yyyy-MM-dd}'}" />
            <Label Text="{Binding GroupCount, StringFormat='Groups: {0}'}" />

            <HorizontalStackLayout Spacing="8">
                <Button
                    Text="Save"
                    ImageSource="ic_save.png"
                    ContentLayout="Left,8"
                    Command="{Binding SaveProfileCommand}" />
                <Button
                    Text="Change Password"
                    IsVisible="{Binding CanChangePassword}"
                    Command="{Binding OpenPasswordChangeCommand}" />
            </HorizontalStackLayout>

            <Button
                Text="Log out"
                ImageSource="ic_logout.png"
                ContentLayout="Left,8"
                BackgroundColor="Transparent"
                TextColor="{AppThemeBinding Light={StaticResource Secondary}, Dark={StaticResource Secondary}}"
                Command="{Binding LogoutCommand}" />

            <Label Text="{Binding StatusMessage}" IsVisible="{Binding ShowStatus}" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
